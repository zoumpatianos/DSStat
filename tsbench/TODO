- If the experiment gets a MULTICHANNEL parser, it should import all channels to the same dataset!
- The experiment should support multiple query generation methods, passed as arguments (or different functions (e.g. run random noise, run from file)

